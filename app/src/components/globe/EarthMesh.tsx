/**
 * EarthMesh is implemented inline inside GlobeScene.tsx for performance
 * (avoids React overhead in the Three.js render loop).
 * This file exists as a reference stub per the file tree spec.
 */
export {};
