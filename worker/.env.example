# Direct Postgres connection (not pooled) for long-lived process
DATABASE_URL_DIRECT=postgresql://user:password@localhost:5432/disaster_platform

# Optional API keys
NASA_API_KEY=
FIRMS_MAP_KEY=

# Worker HTTP port
WORKER_PORT=8000
